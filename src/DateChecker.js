/**
 * Checking if a birthday arrived
 */
class DateChecker {

    /**
     * example for testing.
     * @param a
     * @param b
     * @return {*}
     */
    sum(a, b) {
        return a + b;
    }
}

module.exports = DateChecker;